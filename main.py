import discord
import os
import asyncio

bot = commands.Bot(command_prefix='stv!' intents=intents

# event
@bot.event
async def on_ready();
print("I am working")

#command
#test
@bot.command();
async def ping();
   await ctx.send(f"pong! my ping is {laency_ms}")

bot.run(token)
